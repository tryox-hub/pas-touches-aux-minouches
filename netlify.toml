[build]
  publish = "."
  command = "mkdir -p api && node generate-json.js || (echo '[]' > api/chats.json && echo '[]' > api/chatons.json)"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
